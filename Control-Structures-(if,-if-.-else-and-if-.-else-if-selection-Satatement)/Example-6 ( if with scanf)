#include<stdio.h>
int main ()
{
int x;
 printf("Enter a number to : "); // Enter a number to : 6
 scanf ("%d",&x);
if (x==1)
 {
 x=x+1;
 printf("x=%d\n",x);
 }
else if(x>2 && x<8)
 {
 printf("2 < %d <8 \n",x); // 2 < 6 < 8
 }
else
 printf("x=%d\n",x);
return 0;

}
