/*
Q5: Write a function that collects two vectors of N elements. You can express vectors as a
one- dimensional array.
*/

#include &lt;stdio.h&gt;
int summer(int a[], int b[])
{
for(int i=0;i&lt;5;i++)
{
a[i]=a[i]+b[i];
}
}

int main()
{
int a[5]={1,2,3,4,5},b[5]={2,3,4,5,6};
summer(a,b);
printf(&quot;sum of two arrays are: &quot;);
for(int i=0;i&lt;5;i++)
{
printf(&quot;%3d&quot;, a[i]);
}
}
