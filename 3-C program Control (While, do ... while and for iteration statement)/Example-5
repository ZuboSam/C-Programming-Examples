#include<stdio.h>
int main( void )
{
 int i;
 for (i = 1; i<=5; i++) {
 if (i == 2 || i == 3) {
 printf(" i= %d *** \n" , i); // i= 2 ***
 continue; // i= 3 ***
 }
 printf(" i= %d\n", i); // i= 1
 }
 return 0; // i= 4
} // i= 5
