#include<stdio.h>
int main ()
{
int x,y;
for (x=0 ; x<=5; x=x+2)
{printf("x=%d\n ",x); // x=0
for (y=1 ; y<3; y++) // y=1 y=2
{ // x=2
printf(" y=%d ",y); // y=1 y=2
} // x=4
printf("\n"); // y=1 y=2
}
return 0;
}
